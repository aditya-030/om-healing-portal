import { MessageCircle } from "lucide-react";
interface WhatsAppCardProps {
  phoneNumber: string;
}
const WhatsAppCard = ({
  phoneNumber
}: WhatsAppCardProps) => {
  return;
};
export default WhatsAppCard;